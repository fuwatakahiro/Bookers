<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <!-- CSSファイルの読み込み -->
    <link rel="stylesheet" href="style.css" />
    <title>CARAVAN</title>
  </head>
  <body>
    <body>
    <p id="notice"></p>

<h1>Books</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
<% @lists.each do |list| %>
 <tr>
  <td><%= list.title %></td>
  <td><%= list.body %></td>
   <!-- asのお使い方がわからない-->
  <td><%= link_to "show", "/books/#{list.id}" %></td>
  <td><a class="edit_91789" href="/books/:id/edit">Edit</a></td>
  <td><a data-confirm="Are you sure?" class="destroy_91789" rel="nofollow" data-method="delete" href="/books/91789">Destroy</a></td>
 </tr>
<% end %>
</tbody>
<h2>New book</h2>
<%= form_with model: @list, url: "/books", method: :post do |f| %>
<h4>タイトル</h4>
  <%= f.text_field :title %>
	
  <h4>本文</h4>
  <%= f.text_area :body %>
	
  <%= f.submit 'Create Book' %>
  <% end %>
</body>
  </body>
</html>